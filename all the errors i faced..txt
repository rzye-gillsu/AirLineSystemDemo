
DONE -> check users to make sure not any two users sign up with same username.

-> notifying users when admin changes the flights. -> reading n flightIds: saving n + n flightIds.
                            |ERRORS:
                                - only one flight is removed from user's ticket.
                                - the second user doesn't get notified.

-> whatever you enter which is in the line is counted,
   while sometimes you may only want to search among ticketIds.

-> generating a random flightId for each flight and don't allow anyone to change the flightId.

--------------------------------------------------------------------

-> colorful texts in the terminal.

-> making a parent class that others extend from it.
                                                  | files extend from it.
                                                  |  in views also extend a main menu.